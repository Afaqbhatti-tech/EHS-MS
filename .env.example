# ============================================
# EHS-OS Environment Configuration
# ============================================
# Primary config is in backend/.env (Laravel) and client/.env (Vite)

# ---- MySQL Database ----
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=EHS

# ---- Anthropic Claude AI ----
ANTHROPIC_API_KEY=your-anthropic-api-key

# ---- Default Test Accounts (for seeding only) ----
DEFAULT_MASTER_USERNAME=master
DEFAULT_MASTER_PASSWORD=master
